<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.account.LoginActivity">
    <item
        android:id="@+id/action_all"
        android:orderInCategory="100"
        android:title="@string/menu_favourite_all"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_album"
        android:orderInCategory="100"
        android:title="@string/menu_favourite_album"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_radio"
        android:orderInCategory="100"
        android:title="@string/menu_favourite_radio"
        app:showAsAction="never" />
</menu>
